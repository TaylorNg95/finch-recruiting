import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
      primary: {
        main: '#808080',
      },
      secondary: {
        main: '#808080',
      },
      error: {
        main: '#FF0000',
      },
    }
  });
  
  export default theme;